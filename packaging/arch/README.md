# Arch Linux packaging

This directory preserves the tested Arch/CachyOS packaging recipe as the
starting point for the future `deskunlock` package.

The current recipe may still contain legacy `syauth` package/internal names.
Those are being migrated deliberately so existing installations and state can
be upgraded safely rather than renamed blindly.
